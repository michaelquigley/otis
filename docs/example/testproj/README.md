# testproj

Small demonstration repository for the Otis dummy reviewer.

The code intentionally uses `View` where the example BoK prefers `Lens`, so the
dummy reviewer can produce one deterministic finding.
